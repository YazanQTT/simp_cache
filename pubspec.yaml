name: simp_cache
description: Simple & easy package to cache and retrieve objects as JSON files.
version: 0.0.2
homepage: https://github.com/YazanQTT/simp_cache

environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  encrypted_shared_preferences: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

